const CustomAPIError = require('./custom-error');
const BadRequestError = require('./bad-request');
const UnauthenticatedError = require('./unautenticated');

module.exports = {
    CustomAPIError,
    BadRequestError,
    UnauthenticatedError
};